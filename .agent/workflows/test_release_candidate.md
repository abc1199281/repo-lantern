---
description: Test the current Lantern CLI release against the BatchSmith repository
---

1. go to the target repo
// turbo
1. Initialize Lantern configuration in target repo
// turbo
3. Run the analysis (with auto-confirm)
// turbo
4. assess the generated docs quality
// turbo

Command lines:
1. `cd ../batchsmith/.lantern`
2. `lantern init --overwrite`
3. `lantern run`
4. assess the `./lantern`
5. `cd ../lantern-cli`
